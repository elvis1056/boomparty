import BookingLayoutContent from './BookingLayoutContent';

export default function Layout({ children }: { children: React.ReactNode }) {
  return <BookingLayoutContent>{children}</BookingLayoutContent>;
}
